import math


def calculate_haversine(degrees: float):
    """
    A funtion to calculate the haversine formula in km, provided input in degrees representing latitude and
    longitude.

    parameters
    ----------
    degrees: float
        The input degrees to be converted to distance metric.
    returns
    -------
    lat-long in distance values.
    """
    r = 6371  # radius of earth in metres.


def lat_to_km(degrees: float):
    """
    A function that converts latitude given in degrees to km.

    parameters
    ----------
    degrees: float
        latitude in degrees
    """
    const = 111
    return const * degrees


def lng_to_km(degrees: float):
    """
    A function that converts longitude given in degrees to km.

    parameters
    ----------
    degrees: float
        latitude in degrees
    """
    const = 111.321
    return const * degrees
